# Stops Lombok from searching parent directories for other config files
config.stopBubbling = true

# Tells Lombok to add @lombok.Generated to all generated methods/constructors
lombok.addLombokGeneratedAnnotation = true