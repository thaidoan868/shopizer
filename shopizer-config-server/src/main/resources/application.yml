server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    default: native

management:
  server:
    port: 9000
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health, prometheus
  endpoint:
    health:
      show-details: always
